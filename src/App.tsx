import CourseGoal from './components/CourseGoal';

export default function App() {
  return (
    <main>
      <CourseGoal title='Learn React + TS' desc='Learn it from the ground up' />
    </main>
  );
}
